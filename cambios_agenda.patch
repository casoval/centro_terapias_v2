diff --git a/templates/agenda/calendario.html b/templates/agenda/calendario.html
index 26df37f..8a7c9f6 100644
--- a/templates/agenda/calendario.html
+++ b/templates/agenda/calendario.html
@@ -36,6 +36,23 @@
         padding: 0;
         border: none;
     }
+
+    /* ══════════════════════════════════════════════════════════════════
+       IMPRESIÓN — Reglas globales de la página de Agenda
+       Oculta navegación, filtros y el botón flotante; deja solo el
+       horario (semanal/mensual) ocupando toda la hoja.
+       ══════════════════════════════════════════════════════════════════ */
+    @media print {
+        nav, aside, .sidebar, footer,
+        #header-agenda, .no-print,
+        #modal-editar { display: none !important; }
+
+        body { background: #fff !important; }
+
+        main { max-width: 100% !important; padding: 0 !important; margin: 0 !important; }
+
+        .min-h-screen { min-height: 0 !important; }
+    }
 </style>
 
 <div class="min-h-screen pb-12 relative">
@@ -378,7 +395,7 @@
     </div>
 
     <button onclick="toggleZenMode()" 
-            class="fixed bottom-20 right-6 z-40 bg-slate-800 hover:bg-black text-white w-12 h-12 rounded-full shadow-2xl flex items-center justify-center transition-all hover:scale-110 group border-3 border-white"
+            class="no-print fixed bottom-20 right-6 z-40 bg-slate-800 hover:bg-black text-white w-12 h-12 rounded-full shadow-2xl flex items-center justify-center transition-all hover:scale-110 group border-3 border-white"
             title="Modo Pantalla Completa"
             style="box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.3);">
         <span id="zen-icon" class="text-xl group-hover:rotate-90 transition-transform duration-300">⛶</span>
diff --git a/templates/agenda/partials/calendario_mensual.html b/templates/agenda/partials/calendario_mensual.html
index 807273f..4f378c3 100644
--- a/templates/agenda/partials/calendario_mensual.html
+++ b/templates/agenda/partials/calendario_mensual.html
@@ -59,9 +59,17 @@
                     <input type="checkbox"
                            id="toggle-domingos-mensual"
                            class="w-4 h-4 text-blue-600 rounded focus:ring-offset-0 focus:ring-0 cursor-pointer accent-white"
+                           checked
                            onchange="toggleDomingosMensual(this.checked)">
                     <span class="text-xs text-white font-bold tracking-wide">Dom.</span>
                 </label>
+
+                {# Imprimir horario (respeta filtros y vista actual) #}
+                <button type="button"
+                        onclick="imprimirHorarioMensual()"
+                        class="no-print px-3 py-1.5 rounded-full text-[10px] font-bold bg-white/20 text-white border border-white/30 hover:bg-white/30 transition-all whitespace-nowrap">
+                    🖨️ Imprimir
+                </button>
             </div>
 
         </div>
@@ -537,8 +545,16 @@
 <style>
 #header-dias-mensual, #grid-mensual .grid { transition: none !important; }
 
-.dia-celda { cursor: pointer; transition: all 0.2s ease; }
-.dia-celda:hover:not(.dia-atenuado) { background-color: rgb(249, 250, 251); transform: scale(1.02); }
+/* ⚡ Rendimiento: en meses con muchas sesiones, las filas de semana que
+   quedan fuera de la pantalla (hay que hacer scroll de la página para
+   verlas) no necesitan layout/pintado hasta que se acercan al viewport. */
+#grid-mensual > .grid {
+    content-visibility: auto;
+    contain-intrinsic-size: 0 140px;
+}
+
+.dia-celda { cursor: pointer; }
+.dia-celda:hover:not(.dia-atenuado) { background-color: rgb(249, 250, 251); }
 .dia-celda:hover .dia-numero-header span:not(.bg-blue-600) { background-color: rgb(243, 244, 246); }
 
 /* ── Timeline ── */
@@ -558,6 +574,31 @@
 .tm-sesion.tm-tiny .tm-hora-label,
 .tm-sesion.tm-tiny .tm-servicio-label,
 .tm-sesion.tm-tiny .tm-prof-label { display: none; }
+
+/* ══════════════════════════════════════════════════════════════════════
+   IMPRESIÓN — Horario mensual
+   ══════════════════════════════════════════════════════════════════════ */
+@media print {
+    @page { size: landscape; margin: 8mm; }
+
+    .no-print,
+    #men-vista-timeline,
+    #men-btn-ahora,
+    #men-icono { display: none !important; }
+
+    body, html { background: #fff !important; }
+
+    #men-vista-lista { display: block !important; }
+
+    .rounded-xl, .rounded-lg, .shadow-xl, .shadow-md, .shadow-sm { box-shadow: none !important; border-radius: 0 !important; }
+
+    #grid-mensual > .grid { break-inside: avoid; }
+    .dia-celda { break-inside: avoid; }
+
+    /* Cabecera azul → blanco/negro para no gastar tinta */
+    .bg-gradient-to-r.from-blue-600 { background: #fff !important; color: #000 !important; border-bottom: 2px solid #000; }
+    .bg-gradient-to-r.from-blue-600 * { color: #000 !important; text-shadow: none !important; }
+}
 </style>
 
 
@@ -628,6 +669,17 @@ function menCambiarVista(vista) {
 {% if subvista == 'timeline' %}
 document.addEventListener('DOMContentLoaded', function() { menCambiarVista('timeline'); });
 {% endif %}
+
+// ── Imprimir horario mensual ────────────────────────────────────────────
+// Respeta los filtros aplicados y el estado del toggle "Domingo". Si la
+// vista activa es "timeline" se cambia a "lista" antes de imprimir porque
+// es el formato que mejor encaja en una hoja.
+window.imprimirHorarioMensual = function () {
+    if (_menVistaActiva === 'timeline') {
+        menCambiarVista('lista');
+    }
+    setTimeout(function () { window.print(); }, 150);
+};
 </script>
 
 {# ── Timeline JS ──────────────────────────────────────────────────────── #}
@@ -873,20 +925,8 @@ document.addEventListener('DOMContentLoaded', function() { menCambiarVista('time
         }
     }
 
-    function tmCheckDomingos() {
-        var cols   = document.querySelectorAll('.tm-col-domingo');
-        var toggle = document.getElementById('toggle-domingos-mensual');
-        var tiene  = false;
-        cols.forEach(function(c) {
-            if (c.querySelectorAll('.tm-sesion').length > 0) tiene = true;
-        });
-        if (!tiene) {
-            cols.forEach(function(c) { c.classList.add('tm-col-oculta'); });
-            if (toggle) toggle.checked = false;
-        } else {
-            if (toggle) toggle.checked = true;
-        }
-    }
+    // Domingo se muestra por defecto (checkbox ya viene marcado en el HTML).
+    // Ya no se detecta automáticamente si hay sesiones ni se oculta al cargar.
 
     window.tmInit = function() {
         tmAjustarAlturas();
@@ -895,7 +935,6 @@ document.addEventListener('DOMContentLoaded', function() { menCambiarVista('time
         tmPosicionarSesiones();    // primero: asigna data-startMin/endMin
         tmAplicarAnchoBase();      // luego: calcula anchos según sesiones
         tmActualizarLineaAhora();
-        tmCheckDomingos();
         tmSincronizarScrollHoras();
         setInterval(tmActualizarLineaAhora, 60 * 1000);
     };
@@ -918,24 +957,9 @@ document.addEventListener('DOMContentLoaded', function() { menCambiarVista('time
 
 {# ── Domingos (ambas vistas) ──────────────────────────────────────────── #}
 <script>
-document.addEventListener('DOMContentLoaded', function() {
-    var toggle = document.getElementById('toggle-domingos-mensual');
-
-    // Detectar domingos con sesiones en la vista lista
-    var colsDom = document.querySelectorAll('.columna-domingo-mensual');
-    var tieneSesiones = false;
-    colsDom.forEach(function(col) {
-        if (col.getAttribute('data-dia') === '6' && col.querySelectorAll('.group').length > 0) {
-            tieneSesiones = true;
-        }
-    });
-    if (!tieneSesiones) {
-        ocultarDomingosMensual();
-        if (toggle) toggle.checked = false;
-    } else {
-        if (toggle) toggle.checked = true;
-    }
-});
+// Domingo se muestra por defecto (checkbox ya viene marcado y el grid ya
+// nace en grid-cols-7). Ya no se detecta automáticamente ni se oculta al
+// cargar la página; el usuario decide con el toggle.
 
 function toggleDomingosMensual(mostrar) {
     if (mostrar) { mostrarDomingosMensual(); } else { ocultarDomingosMensual(); }
diff --git a/templates/agenda/partials/calendario_mensual_timeline.html b/templates/agenda/partials/calendario_mensual_timeline.html
index 6b2b8c5..3c9ff8a 100644
--- a/templates/agenda/partials/calendario_mensual_timeline.html
+++ b/templates/agenda/partials/calendario_mensual_timeline.html
@@ -44,7 +44,7 @@
 
                 {# Toggle domingos #}
                 <label class="flex items-center gap-2 cursor-pointer bg-white/10 px-3 py-1.5 rounded-lg border border-white/20 hover:bg-white/20 transition-all text-xs font-bold">
-                    <input type="checkbox" id="tm-toggle-domingo" onchange="tmToggleDomingo(this.checked)" class="accent-white w-3.5 h-3.5">
+                    <input type="checkbox" id="tm-toggle-domingo" checked onchange="tmToggleDomingo(this.checked)" class="accent-white w-3.5 h-3.5">
                     Dom.
                 </label>
 
@@ -518,25 +518,8 @@
         wrapper.scrollTo({ left: Math.max(0, scrollLeft), behavior: 'smooth' });
     }
 
-    // ── Detectar si hay sesiones en domingos ─────────────────────────────────
-    function checkDomingos() {
-        const colsDom  = document.querySelectorAll('.tm-col-domingo');
-        const toggle   = document.getElementById('tm-toggle-domingo');
-        let tieneSesiones = false;
-
-        colsDom.forEach(col => {
-            if (col.querySelectorAll('.tm-sesion').length > 0) {
-                tieneSesiones = true;
-            }
-        });
-
-        if (!tieneSesiones) {
-            colsDom.forEach(col => col.classList.add('tm-col-oculta'));
-            if (toggle) toggle.checked = false;
-        } else {
-            if (toggle) toggle.checked = true;
-        }
-    }
+    // Domingo se muestra por defecto (checkbox ya viene marcado en el HTML).
+    // Ya no se detecta automáticamente ni se oculta al cargar.
 
     // ── Sincronizar scroll vertical: sidebar sigue al wrapper ───────────────
     function sincronizarScrollHoras() {
@@ -578,7 +561,6 @@
         dibujarLineasFondo();
         posicionarSesiones();
         actualizarLineaAhora();
-        checkDomingos();
         sincronizarScrollHoras();
 
         setInterval(actualizarLineaAhora, 60 * 1000);
diff --git a/templates/agenda/partials/calendario_semanal.html b/templates/agenda/partials/calendario_semanal.html
index 1faac87..f81850b 100644
--- a/templates/agenda/partials/calendario_semanal.html
+++ b/templates/agenda/partials/calendario_semanal.html
@@ -65,9 +65,17 @@
                     <input type="checkbox"
                            id="toggle-domingo"
                            class="w-4 h-4 accent-white cursor-pointer"
+                           checked
                            onchange="semToggleDomingo(this.checked)">
                     <span class="text-xs text-white font-bold tracking-wide">Dom.</span>
                 </label>
+
+                {# Imprimir horario (respeta filtros y vista actual) #}
+                <button type="button"
+                        onclick="imprimirHorarioSemanal()"
+                        class="no-print px-3 py-1.5 rounded-full text-[10px] font-bold bg-white/20 text-white border border-white/30 hover:bg-white/30 transition-all whitespace-nowrap">
+                    🖨️ Imprimir
+                </button>
             </div>
         </div>
     </div>
@@ -80,9 +88,10 @@
 
         <div id="header-dias" class="flex w-full bg-gray-200 border-b border-gray-200 divide-x divide-gray-200 flex-shrink-0">
             {% for dia in calendario_data.dias %}
-            <div class="dia-header flex-1 min-w-0 transition-all duration-500 ease-in-out py-2.5 px-1 text-center bg-gray-50 flex flex-col items-center justify-center cursor-pointer hover:bg-blue-50 relative overflow-hidden
+            <div class="dia-header flex-1 min-w-0 py-2.5 px-1 text-center bg-gray-50 flex flex-col items-center justify-center cursor-pointer hover:bg-blue-50 relative overflow-hidden
                         {% if dia.es_hoy %}bg-blue-50/50{% endif %}
                         {% if forloop.last %}columna-domingo border-r-0{% endif %}"
+                 style="transition: flex 0.4s cubic-bezier(0.4,0,0.2,1), background-color 0.3s ease;"
                  data-dia="{{ forloop.counter0 }}"
                  onclick="toggleZoomDia({{ forloop.counter0 }})"
                  title="Click para enfocar este día">
@@ -103,11 +112,11 @@
 
         <div id="grid-sesiones" class="flex w-full bg-gray-200 divide-x divide-gray-200">
             {% for dia in calendario_data.dias %}
-            <div class="columna-dia flex-1 min-w-0 transition-all duration-500 ease-in-out bg-white p-1.5 flex flex-col gap-1.5 relative overflow-y-auto overflow-x-hidden
+            <div class="columna-dia flex-1 min-w-0 bg-white p-1.5 flex flex-col gap-1.5 relative overflow-y-auto overflow-x-hidden
                         {% if dia.es_hoy %}bg-blue-50/10{% endif %}
                         {% if forloop.last %}columna-domingo{% endif %}"
                  data-dia="{{ forloop.counter0 }}"
-                 style="scrollbar-width: thin; scrollbar-color: #cbd5e1 #f1f5f9;">
+                 style="scrollbar-width: thin; scrollbar-color: #cbd5e1 #f1f5f9; transition: flex 0.4s cubic-bezier(0.4,0,0.2,1), background-color 0.3s ease;">
 
                 <div class="content-wrapper h-full w-full transition-opacity duration-300"
                      onclick="handleColumnClick(event, {{ forloop.counter0 }})">
@@ -418,6 +427,15 @@
 
 
 <style>
+/* ⚡ Rendimiento: dentro de cada columna de día (con scroll propio), las
+   tarjetas de sesión que quedan fuera del área visible no necesitan layout
+   ni pintado hasta que se hagan visibles. Con muchas sesiones por día esto
+   evita recalcular todo el árbol en cada scroll/hover. */
+.columna-dia .group {
+    content-visibility: auto;
+    contain-intrinsic-size: 0 60px;
+}
+
 .tl-col-oculta { display: none !important; }
 .tl-col-dia {
     transition: flex 0.3s cubic-bezier(0.4,0,0.2,1),
@@ -434,6 +452,37 @@
 .tl-sesion.tl-tiny .tl-t-srv,
 .tl-sesion.tl-tiny .tl-t-prof { display: none; }
 .tl-sesion.tl-tiny .tl-t-pac { font-size: 7px; }
+
+/* ══════════════════════════════════════════════════════════════════════
+   IMPRESIÓN — Horario semanal
+   ══════════════════════════════════════════════════════════════════════ */
+@media print {
+    @page { size: landscape; margin: 8mm; }
+
+    .no-print,
+    #sem-vista-timeline,
+    #btn-restaurar,
+    #sem-btn-ahora,
+    .dia-header .absolute,
+    .group-hover\:opacity-100,
+    #sem-icono { display: none !important; }
+
+    body, html { background: #fff !important; }
+
+    #sem-vista-lista { display: block !important; }
+
+    .rounded-xl, .rounded-lg, .shadow-xl, .shadow-md, .shadow-sm { box-shadow: none !important; border-radius: 0 !important; }
+
+    #header-dias, .columna-dia {
+        break-inside: avoid;
+    }
+
+    .group { break-inside: avoid; }
+
+    /* Cabecera azul → compacta y en blanco/negro para no gastar tinta */
+    .bg-gradient-to-r.from-blue-600 { background: #fff !important; color: #000 !important; border-bottom: 2px solid #000; }
+    .bg-gradient-to-r.from-blue-600 * { color: #000 !important; text-shadow: none !important; }
+}
 </style>
 
 
@@ -451,6 +500,18 @@
     var tlIniciado  = false;
 
     // ── Toggle de vista (botón del header) ────────────────────────────────
+    // ── Imprimir horario semanal ──────────────────────────────────────────
+    // Imprime respetando los filtros aplicados (profesional, servicio,
+    // sucursal, estado, etc.) y el estado actual del toggle "Domingo".
+    // Si la vista activa es "timeline" se cambia a "lista" antes de imprimir
+    // porque es el formato que mejor encaja en una hoja.
+    window.imprimirHorarioSemanal = function () {
+        if (vistaActual === 'timeline') {
+            semCambiarVista('lista');
+        }
+        setTimeout(function () { window.print(); }, 150);
+    };
+
     window.semCambiarVista = function (vista) {
         vistaActual = vista;
         var listaEl     = document.getElementById('sem-vista-lista');
@@ -576,28 +637,10 @@
 
     var diaZoomed = -1;
 
-    // Detectar domingo al cargar
-    document.addEventListener('DOMContentLoaded', function () {
-        var toggle = document.getElementById('toggle-domingo');
-        var columnasDomingo = document.querySelectorAll('.columna-domingo');
-        var tieneSesiones = false;
-        columnasDomingo.forEach(function(col) {
-            if (col.getAttribute('data-dia') === '6' && col.querySelectorAll('.group').length > 0) {
-                tieneSesiones = true;
-            }
-        });
-        if (!tieneSesiones) {
-            ocultarDomingo();
-            if (toggle) toggle.checked = false;
-        } else {
-            if (toggle) toggle.checked = true;
-        }
-    });
-
-    function ocultarDomingo() {
-        document.querySelectorAll('.columna-domingo').forEach(function(c){ c.style.display='none'; });
-        document.querySelectorAll('.tl-col-domingo').forEach(function(c){ c.classList.add('tl-col-oculta'); });
-    }
+    // Domingo se muestra por defecto (checkbox ya viene marcado en el HTML).
+    // El usuario lo oculta manualmente con el toggle; ya no se detecta ni se
+    // oculta automáticamente al cargar (evita el parpadeo y el trabajo extra
+    // de recorrer todas las sesiones al entrar a la vista).
 
     // Mantener compatibilidad con toggleDomingo si se llama desde otro sitio
     window.toggleDomingo = window.semToggleDomingo;
diff --git a/templates/agenda/partials/calendario_semanal_timeline.html b/templates/agenda/partials/calendario_semanal_timeline.html
index f40d249..0611e7b 100644
--- a/templates/agenda/partials/calendario_semanal_timeline.html
+++ b/templates/agenda/partials/calendario_semanal_timeline.html
@@ -40,7 +40,7 @@
 
                 {# Toggle domingo #}
                 <label class="flex items-center gap-2 cursor-pointer bg-white/10 px-3 py-1.5 rounded-lg border border-white/20 hover:bg-white/20 transition-all text-xs font-bold">
-                    <input type="checkbox" id="tl-toggle-domingo" onchange="timelineToggleDomingo(this.checked)" class="accent-white w-3.5 h-3.5">
+                    <input type="checkbox" id="tl-toggle-domingo" checked onchange="timelineToggleDomingo(this.checked)" class="accent-white w-3.5 h-3.5">
                     Dom.
                 </label>
 
@@ -428,19 +428,9 @@
     // (Opcional: Click en zona vacía → se podría abrir modal de nueva sesión)
     // Por ahora solo visual via CSS hover en tl-slot-libre.
 
-    // ── Detectar si hay domingos con sesiones ────────────────────────────────
-    function checkDomingo() {
-        const colDom = document.querySelector('.tl-col-domingo');
-        if (!colDom) return;
-        const tieneSesiones = colDom.querySelectorAll('.tl-sesion').length > 0;
-        const toggle = document.getElementById('tl-toggle-domingo');
-        if (!tieneSesiones) {
-            colDom.classList.add('tl-col-oculta');
-            if (toggle) toggle.checked = false;
-        } else {
-            if (toggle) toggle.checked = true;
-        }
-    }
+    // Domingo se muestra por defecto (checkbox ya viene marcado en el HTML).
+    // El usuario lo oculta manualmente con el toggle; ya no se detecta ni se
+    // oculta automáticamente al cargar.
 
     // ── Tamaño de columna hora ────────────────────────────────────────────────
     function ajustarAlturaHoraCol() {
@@ -458,7 +448,6 @@
         dibujarLineasFondo();
         posicionarSesiones();
         actualizarLineaAhora();
-        checkDomingo();
 
         // Actualizar línea "ahora" cada minuto
         setInterval(actualizarLineaAhora, 60 * 1000);
