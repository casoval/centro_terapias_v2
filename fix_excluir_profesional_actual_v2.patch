diff --git a/agenda/views.py b/agenda/views.py
index c6d61a9..0a59829 100644
--- a/agenda/views.py
+++ b/agenda/views.py
@@ -5203,6 +5203,16 @@ def procesar_cambiar_profesional_mes(request, paciente_id):
                 errores.append(f"{sesion.fecha.strftime('%d/%m/%Y')} {sesion.hora_inicio.strftime('%H:%M')} — sin permiso sobre esa sucursal")
                 continue
 
+            # Ya tiene asignado ese mismo profesional (el frontend ya lo excluye
+            # de las opciones, esto es solo defensa ante llamadas directas a la API)
+            if sesion.profesional_id == nuevo_profesional.id:
+                omitidas += 1
+                errores.append(
+                    f"{sesion.fecha.strftime('%d/%m/%Y')} {sesion.hora_inicio.strftime('%H:%M')} "
+                    f"— ya estaba asignada a {nuevo_profesional.nombre} {nuevo_profesional.apellido}"
+                )
+                continue
+
             # El nuevo profesional debe ofrecer ese servicio
             if sesion.servicio_id and not nuevo_profesional.servicios.filter(id=sesion.servicio_id).exists():
                 omitidas += 1
diff --git a/templates/agenda/modal_cambiar_profesional_mes.html b/templates/agenda/modal_cambiar_profesional_mes.html
index 4a145b3..195e8ca 100644
--- a/templates/agenda/modal_cambiar_profesional_mes.html
+++ b/templates/agenda/modal_cambiar_profesional_mes.html
@@ -152,11 +152,24 @@
 
         avisoMezcla.classList.add('hidden');
         const servicioId = Array.from(servicioIds)[0];
-        const profs = PROF_MAP[servicioId] || [];
+        const profsServicio = PROF_MAP[servicioId] || [];
+
+        // No tiene sentido ofrecer como "nuevo profesional" a quien ya está
+        // asignado a alguna de las sesiones marcadas (sería "cambiar" a la
+        // misma persona). Si hay sesiones con distintos profesionales
+        // actuales, se excluyen todos para garantizar que la reasignación
+        // realmente cambie a todas las sesiones seleccionadas.
+        const idsSesionesMarcadas = new Set(marcados.map(c => parseInt(c.dataset.id)));
+        const profesionalesActuales = new Set(
+            SESIONES.filter(s => idsSesionesMarcadas.has(s.id)).map(s => s.profesional_id)
+        );
+        const profs = profsServicio.filter(p => !profesionalesActuales.has(p.id));
 
         if (!profs.length) {
             selectProf.disabled = true;
-            selectProf.innerHTML = '<option value="">Sin profesionales activos para este servicio</option>';
+            selectProf.innerHTML = profsServicio.length
+                ? '<option value="">No hay otro profesional activo disponible para este servicio</option>'
+                : '<option value="">Sin profesionales activos para este servicio</option>';
             btnConfirmar.disabled = true;
             return;
         }
