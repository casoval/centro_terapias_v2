diff --git a/agenda/informe_horario_pdf.py b/agenda/informe_horario_pdf.py
index c697b16..6755c34 100644
--- a/agenda/informe_horario_pdf.py
+++ b/agenda/informe_horario_pdf.py
@@ -70,8 +70,8 @@ MESES = ['', 'Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio',
 PAGE_W, PAGE_H = landscape(letter)
 MARGIN_L = 1.3 * cm
 MARGIN_R = 1.3 * cm
-HEADER_H = 3.15 * cm  # alto del encabezado de marca (logo + nombre + 2 sucursales)
-MARGIN_T = HEADER_H + 0.65 * cm   # deja espacio para el encabezado dibujado por página
+HEADER_H = 1.75 * cm  # alto del encabezado de marca, ajustado al mínimo que entra el texto
+MARGIN_T = HEADER_H + 0.55 * cm   # deja espacio para el encabezado dibujado por página
 MARGIN_B = 1.2 * cm
 CONTENT_W = PAGE_W - MARGIN_L - MARGIN_R
 
@@ -368,12 +368,14 @@ def generar_horario_pdf(
             partes.append(f"Sucursal: {sucursal_unica}")
         linea_contexto = "   •   ".join(partes)
 
+    LOGO_H = 1.3 * cm   # tamaño fijo del logo, no depende del alto de la franja
+
     def _draw_encabezado(c, _doc):
         _grad(c, 0, PAGE_H - HEADER_H, PAGE_W, HEADER_H, C_AZUL_OSC, C_AZUL_PRI)
 
         lp = _logo()
-        lh = (HEADER_H - 0.5 * cm) / 2   # logo a la mitad del tamaño anterior
-        lw = lh
+        lh = LOGO_H
+        lw = LOGO_H
         lx = MARGIN_L
         ly = PAGE_H - HEADER_H / 2 - lh / 2   # centrado verticalmente en la franja
         if lp:
@@ -384,24 +386,26 @@ def generar_horario_pdf(
                 pass
 
         tx = lx + lw + 0.35 * cm
+        y_centro = PAGE_H - HEADER_H / 2   # centro vertical de la franja
+
         c.setFillColor(colors.white)
         c.setFont("Helvetica-Bold", 11.5)
-        c.drawString(tx, PAGE_H - 0.75 * cm, NOMBRE_CENTRO)
+        c.drawString(tx, y_centro + 0.32 * cm, NOMBRE_CENTRO)
 
         # Ambas sucursales, una línea cada una (nombre · dirección · teléfono)
         c.setFont("Helvetica", 6.9)
-        y_suc = PAGE_H - 1.35 * cm
+        y_suc = y_centro - 0.1 * cm
         for suc in SUCURSALES_CENTRO:
             c.drawString(
                 tx, y_suc,
                 f"{suc['nombre']} · {suc['direccion']}  ·  Tel. {suc['telefono']}"
             )
-            y_suc -= 0.42 * cm
+            y_suc -= 0.4 * cm
 
         c.setFont("Helvetica-Bold", 12)
-        c.drawRightString(PAGE_W - MARGIN_R, PAGE_H - 0.75 * cm, titulo_vista)
+        c.drawRightString(PAGE_W - MARGIN_R, y_centro + 0.27 * cm, titulo_vista)
         c.setFont("Helvetica", 8.5)
-        c.drawRightString(PAGE_W - MARGIN_R, PAGE_H - 1.25 * cm, subtitulo)
+        c.drawRightString(PAGE_W - MARGIN_R, y_centro - 0.33 * cm, subtitulo)
 
         if linea_contexto:
             c.setFillColor(C_TEXTO)
